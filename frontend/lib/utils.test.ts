// Placeholder to keep vitest from failing with "no test files found"
import { describe, it, expect } from "vitest"

describe("utils", () => {
  it("placeholder", () => {
    expect(true).toBe(true)
  })
})
